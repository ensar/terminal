import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    main: {
      white: '#fff',
      red: '#ff0000',
      blue: '#12a6eb',
      green: '#C6FFC8',
      darkBlue: '#1BA6E7',
      darkRed: '#D41922',
      darkGreen: '#AEE0B0',
    },
  },
});
